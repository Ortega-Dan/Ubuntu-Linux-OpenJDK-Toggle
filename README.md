# Ubuntu-Linux-OpenJDK-Toggle

Toggles between OpenJDK 8 and 11

requires sudo, because update-java-alternatives command requires sudo


To install do:
sudo bash deploy.sh

To toggle between OpenJDK 8 and 11 do:
sudo toggleJava
